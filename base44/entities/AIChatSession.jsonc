{
  "name": "AIChatSession",
  "type": "object",
  "properties": {
    "ownerUserId": {
      "type": "string"
    },
    "auditId": {
      "type": "number"
    },
    "title": {
      "type": "string"
    },
    "projectId": {
      "type": "string"
    },
    "feature": {
      "type": "string",
      "enum": [
        "editor",
        "coach",
        "research"
      ]
    }
  },
  "required": [
    "title",
    "feature"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}