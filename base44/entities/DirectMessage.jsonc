{
  "name": "DirectMessage",
  "type": "object",
  "properties": {
    "ownerUserId": {
      "type": "string"
    },
    "auditId": {
      "type": "number"
    },
    "senderId": {
      "type": "string"
    },
    "receiverId": {
      "type": "string"
    },
    "content": {
      "type": "string"
    }
  },
  "required": [
    "senderId",
    "receiverId",
    "content"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "data.senderId": "{{user.id}}"
        },
        {
          "data.receiverId": "{{user.id}}"
        },
        {
          "created_by_id": "{{user.id}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.senderId": "{{user.id}}"
        },
        {
          "created_by_id": "{{user.id}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.senderId": "{{user.id}}"
        },
        {
          "created_by_id": "{{user.id}}"
        }
      ]
    }
  }
}