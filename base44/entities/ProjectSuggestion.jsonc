{
  "name": "ProjectSuggestion",
  "type": "object",
  "properties": {
    "projectId": {
      "type": "string",
      "maxLength": 4000000
    },
    "projectName": {
      "type": "string",
      "maxLength": 4000000
    },
    "title": {
      "type": "string",
      "maxLength": 4000000
    },
    "description": {
      "type": "string",
      "maxLength": 4000000
    },
    "userId": {
      "type": "string",
      "maxLength": 4000000
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "approved",
        "rejected"
      ],
      "default": "pending",
      "maxLength": 4000000
    }
  },
  "required": [
    "projectId",
    "title",
    "description",
    "userId"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "data.userId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.userId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.userId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}