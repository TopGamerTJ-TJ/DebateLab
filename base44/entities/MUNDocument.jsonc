{
  "name": "MUNDocument",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "maxLength": 4000000
    },
    "type": {
      "type": "string",
      "enum": [
        "position_paper",
        "draft_resolution",
        "working_paper",
        "amendment",
        "speech",
        "country_profile",
        "committee_prep"
      ],
      "maxLength": 4000000
    },
    "country": {
      "type": "string",
      "maxLength": 4000000
    },
    "committee": {
      "type": "string",
      "maxLength": 4000000
    },
    "topic": {
      "type": "string",
      "maxLength": 4000000
    },
    "content": {
      "type": "string",
      "maxLength": 4000000
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string",
        "maxLength": 4000000
      }
    },
    "isFavorite": {
      "type": "boolean"
    },
    "tournamentId": {
      "type": "string",
      "maxLength": 4000000
    }
  },
  "required": [
    "title",
    "type"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}