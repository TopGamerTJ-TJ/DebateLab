{
  "name": "Friendship",
  "type": "object",
  "properties": {
    "requesterId": {
      "type": "string",
      "maxLength": 4000000
    },
    "requesterName": {
      "type": "string",
      "maxLength": 4000000
    },
    "recipientId": {
      "type": "string",
      "maxLength": 4000000
    },
    "recipientName": {
      "type": "string",
      "maxLength": 4000000
    },
    "status": {
      "type": "string",
      "enum": [
        "pending",
        "accepted",
        "rejected"
      ],
      "default": "pending",
      "maxLength": 4000000
    }
  },
  "required": [
    "requesterId",
    "recipientId"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "data.requesterId": "{{user.id}}"
        },
        {
          "data.recipientId": "{{user.id}}"
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.requesterId": "{{user.id}}"
        },
        {
          "data.recipientId": "{{user.id}}"
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.requesterId": "{{user.id}}"
        },
        {
          "data.recipientId": "{{user.id}}"
        }
      ]
    }
  }
}