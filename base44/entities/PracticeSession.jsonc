{
  "name": "PracticeSession",
  "type": "object",
  "properties": {
    "ownerUserId": {
      "type": "string",
      "maxLength": 4000000
    },
    "auditId": {
      "type": "number"
    },
    "format": {
      "type": "string",
      "maxLength": 4000000
    },
    "side": {
      "type": "string",
      "maxLength": 4000000
    },
    "resolution": {
      "type": "string",
      "maxLength": 4000000
    },
    "difficulty": {
      "type": "string",
      "maxLength": 4000000
    },
    "transcript": {
      "type": "string",
      "maxLength": 4000000
    },
    "rfd": {
      "type": "string",
      "maxLength": 4000000
    },
    "winner": {
      "type": "string",
      "maxLength": 4000000
    },
    "speakerPoints": {
      "type": "number"
    },
    "strengths": {
      "type": "array",
      "items": {
        "type": "string",
        "maxLength": 4000000
      }
    },
    "weaknesses": {
      "type": "array",
      "items": {
        "type": "string",
        "maxLength": 4000000
      }
    },
    "keyVotingIssues": {
      "type": "array",
      "items": {
        "type": "string",
        "maxLength": 4000000
      }
    },
    "notes": {
      "type": "string",
      "maxLength": 4000000
    },
    "duration": {
      "type": "number"
    }
  },
  "required": [
    "format",
    "side"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}