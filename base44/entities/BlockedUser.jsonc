{
  "name": "BlockedUser",
  "type": "object",
  "properties": {
    "blockerId": {
      "type": "string",
      "maxLength": 4000000
    },
    "blockedUserId": {
      "type": "string",
      "maxLength": 4000000
    },
    "blockedUserName": {
      "type": "string",
      "maxLength": 4000000
    },
    "reason": {
      "type": "string",
      "maxLength": 4000000
    }
  },
  "required": [
    "blockerId",
    "blockedUserId"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "data.blockerId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.blockerId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.blockerId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}