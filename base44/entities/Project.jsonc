{
  "name": "Project",
  "type": "object",
  "properties": {
    "ownerUserId": {
      "type": "string",
      "maxLength": 4000000
    },
    "auditId": {
      "type": "number"
    },
    "name": {
      "type": "string",
      "maxLength": 4000000
    },
    "description": {
      "type": "string",
      "maxLength": 4000000
    },
    "format": {
      "type": "string",
      "enum": [
        "parliamentary",
        "public_forum",
        "model_un",
        "model_congress"
      ],
      "maxLength": 4000000
    },
    "resolution": {
      "type": "string",
      "maxLength": 4000000
    },
    "side": {
      "type": "string",
      "maxLength": 4000000
    },
    "color": {
      "type": "string",
      "maxLength": 4000000
    },
    "notes": {
      "type": "string",
      "maxLength": 4000000
    },
    "isArchived": {
      "type": "boolean"
    },
    "aiMode": {
      "type": "string",
      "enum": [
        "default",
        "full",
        "dampened"
      ],
      "default": "default",
      "maxLength": 4000000
    },
    "folderId": {
      "type": "string",
      "maxLength": 4000000
    },
    "conferenceProfileId": {
      "type": "string",
      "maxLength": 4000000
    },
    "whitelist": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "maxLength": 4000000
          },
          "label": {
            "type": "string",
            "maxLength": 4000000
          }
        }
      }
    },
    "blacklist": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "url": {
            "type": "string",
            "maxLength": 4000000
          },
          "label": {
            "type": "string",
            "maxLength": 4000000
          }
        }
      }
    },
    "strictMode": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [
    "name"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}