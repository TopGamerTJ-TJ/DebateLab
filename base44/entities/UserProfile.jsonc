{
  "name": "UserProfile",
  "type": "object",
  "properties": {
    "ownerUserId": {
      "type": "string",
      "maxLength": 4000000
    },
    "ownerEmail": {
      "type": "string",
      "maxLength": 4000000
    },
    "auditId": {
      "type": "number"
    },
    "displayName": {
      "type": "string",
      "maxLength": 4000000
    },
    "school": {
      "type": "string",
      "maxLength": 4000000
    },
    "gradeLevel": {
      "type": "string",
      "maxLength": 4000000
    },
    "preferredFormat": {
      "type": "string",
      "maxLength": 4000000
    },
    "skillLevel": {
      "type": "string",
      "enum": [
        "new",
        "beginner",
        "intermediate",
        "advanced",
        "expert"
      ],
      "maxLength": 4000000
    },
    "bio": {
      "type": "string",
      "maxLength": 4000000
    },
    "totalPracticeSessions": {
      "type": "number"
    },
    "totalContentions": {
      "type": "number"
    },
    "totalTournaments": {
      "type": "number"
    },
    "wins": {
      "type": "number"
    },
    "losses": {
      "type": "number"
    },
    "learnTabEnabled": {
      "type": "boolean",
      "default": false
    },
    "learningPathType": {
      "type": "string",
      "enum": [
        "debate",
        "model_un",
        "model_congress"
      ],
      "maxLength": 4000000
    },
    "learningPathFormat": {
      "type": "string",
      "enum": [
        "public_forum",
        "parliamentary"
      ],
      "maxLength": 4000000
    },
    "defaultAiMode": {
      "type": "string",
      "enum": [
        "full",
        "dampened"
      ],
      "default": "full",
      "maxLength": 4000000
    },
    "notificationsEnabled": {
      "type": "boolean",
      "default": true
    },
    "themeMode": {
      "type": "string",
      "enum": [
        "light",
        "dark"
      ],
      "maxLength": 4000000
    },
    "customColors": {
      "type": "object"
    },
    "defaultMinKeyFacts": {
      "type": "number",
      "default": 3
    },
    "defaultMinLogicPoints": {
      "type": "number",
      "default": 3
    },
    "defaultMinContentions": {
      "type": "number",
      "default": 2
    },
    "delegateOfUN": {
      "type": "boolean",
      "default": false
    }
  },
  "required": [],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "data.ownerEmail": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "data.ownerEmail": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "data.ownerEmail": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}