{
  "name": "OtherDocument",
  "type": "object",
  "properties": {
    "ownerUserId": {
      "type": "string"
    },
    "title": {
      "type": "string"
    },
    "docLabel": {
      "type": "string",
      "description": "Free-form label for the document type, e.g. 'Cross-Ex Prep', 'Briefing Memo'"
    },
    "description": {
      "type": "string",
      "description": "The instructions the user gave the AI for what to generate"
    },
    "content": {
      "type": "string"
    },
    "projectId": {
      "type": "string"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "isFavorite": {
      "type": "boolean"
    }
  },
  "required": [
    "title",
    "content"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "created_by_id": "{{user.id}}"
        },
        {
          "data.ownerUserId": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}